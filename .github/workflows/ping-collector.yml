name: Ping Collector

on:
  schedule:
    - cron: '*/10 * * * *'
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - run: curl -s -o /dev/null -w '%{http_code}' https://bus-dashboard-collector.onrender.com
